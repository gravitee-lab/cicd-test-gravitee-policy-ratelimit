!.gitignore

build/
target/
.idea/
.DS_Store
*.iml

*.class

# Mobile Tools for Java (J2ME)
.mtj.tmp/

# virtual machine crash logs, see http://www.java.com/en/download/help/error_hotspot.xml
hs_err_pid*

# Eclipse
.settings/
.classpath
.project
# ingore the [settings.xml] which contains secrets (password to artifactory)
settings.xml
# ingore the [settings.xml] which contains secrets (password to artifactory)
settings.xml
# ingore the [settings.xml] which contains secrets (password to artifactory)
settings.xml
# ingore the [settings.xml] which contains secrets (password to artifactory)
settings.xml
# ingore the [settings.xml] which contains secrets (password to artifactory)
settings.xml
# ingore the [settings.xml] which contains secrets (password to artifactory)
settings.xml
